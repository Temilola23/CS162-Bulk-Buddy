# Bulk Buddy -- Backend

The backend API for Bulk Buddy, built with Python (Flask).

## Setup

*Instructions will be added once the backend framework is initialized.*

## Structure

```
backend/
├── tests/          # Backend tests
└── ...             # API source files (to be initialized)
```

## Running Locally

*TBD*

## Testing

Run backend tests:

```bash
cd backend
# TBD: add test command
```

## Database

The database configuration, models, and migrations live within this backend directory.
Managed through SQLAlchemy ORM.
